import { LightningElement,api } from 'lwc';

export default class PractiseDecorators extends LightningElement {

   @api msg="hii nag raj";
    
   /* handleChange(event){
        this.msg=event.target.value;
        console.log(this.msg);
     } --> */
}